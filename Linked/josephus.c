#include<stdio.h>
#include<stdlib.h>

typedef struct node_t {
    int data;
    struct node_t * next;

} node_t;

node_t * Josephus ( node_t  * head, int k ) {
    if ( head == NULL ) {
        return NULL;
    } 

}   

void main ()  {
    printf("%d", (-2 % 4));
}